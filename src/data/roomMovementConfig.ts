import type { RoomWalkableArea } from '../types/game';

// All movement coordinates are normalized percentages of the room frame.
export const roomWalkableArea: RoomWalkableArea = {
  minX: 24,
  maxX: 76,
  minY: 64,
  maxY: 86,
};
